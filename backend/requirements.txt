# Backend runtime dependencies — slimmed for fast, reliable Render builds.
# Removed pandas/numpy/boto3/cryptography + lint & dev tools that the app never imports
# (verified: server.py + email_reset.py import none of them). This cuts build time and
# the number of PyPI downloads (which is what caused the flaky tzdata timeout).
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pymongo==4.9.2
motor==3.7.1
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
pyotp>=2.9.0
qrcode[pil]>=7.4.2
tzdata>=2024.2
python-multipart>=0.0.9
requests>=2.31.0          # Resend API calls (email_service) + backend tests

# test-only
pytest>=8.0.0
